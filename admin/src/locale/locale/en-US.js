/**
 * @desc 语言包
 */

let locale = {
    'this is the message default title': 'this is the message default title',
    'this is the default message': 'this is the default message'
};

export default locale;
